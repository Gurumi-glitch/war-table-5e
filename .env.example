# Convex deployment URL (run `npx convex dev` to get yours, then copy here).
# Used by the frontend to connect to the backend.
VITE_CONVEX_URL=

# Convex backend uses this locally; set by `npx convex dev`.
CONVEX_DEPLOYMENT=
